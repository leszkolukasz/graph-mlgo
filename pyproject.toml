[project]
name = "graph-mlgo"
version = "0.1.0"
description = "GraphMLGO - Topology-aware Machine Learning Guided Compiler Optimizations Framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium>=1.3.0",
]
